# 2026-09-21 — builderdecade: internal-link (orphan) sweep + ZH Traditional fix

**Profile:** build · **Trigger:** Daily Driver Today #3 (Builderdecade DECIDE card) → Vincent chose
**A** (internal-link sweep, lever #2) **and B** (`what-your-agent-can-reach`).

This changelog covers **A**. B is a separate follow-up commit.

**Rollback tag:** `rollback-pre-orphan-sweep-2026-09-21` (HEAD `eba52cb` before edits)

## Why this was the right lever

The Stage 1 v3 foothold pass diagnosed builderdecade as the *opposite* of traindecade:

```
traindecade    footholds DEEP      pos 55–80     → can't reach light
builderdecade  footholds SHALLOW   pos 7.1/16.1  → reaches light, starved of water
               (10–12 impressions each = below any usable sample)
```

The v3 queue's honest lever menu ranks **internal links at #2** — described as *"free, instant, fully
in our control (fix orphans FIRST)"*. That is what this commit does.

## Finding 1 — 23 of 38 posts were orphans (60% of the site), identically in both languages

Built a link graph (`/tmp/link_graph.py`, read-only) resolving every `](/posts/slug/)` reference per
language. Result:

```
BEFORE                              EN    ZH
posts with 0 inbound internal links  23    23
```

The 23 included **every post that holds real search position**:

```
solo-saas-pricing                    12 impr  pos 16.1  ← page 2, orphaned
when-to-hire-your-first-contractor   10 impr  pos  7.1  ← page 1, orphaned ★
solo-founder-vs-cofounder            20 impr  pos 55.1  ← biggest pool, orphaned
no-code-vibe-code-real-code          17 impr  pos 70.9  ← biggest cluster, orphaned
seo-for-indie-hackers                 6 impr  pos  5.3  ← page 1, orphaned
one-person-team                       6 impr  pos  3.2  ← page 1, orphaned
ship-one-tiny-thing (ZH)              1 impr  pos  1.0  ← ONLY converter on site
```

Same defect class as the traindecade `whey-vs-beef-protein` orphan found 2026-09-15: the pages rank
on content alone because nothing feeds them.

## Fix — contextual links, not a link dump

⚠️ The mechanical approach (link every orphan from every cluster sibling) was **rejected** — it
produces keyword-stuffed link blocks that read as spam and help nobody. Instead, **one genuinely
relevant sentence per link**, appended before each post's `## Sources` block, in both languages.

```
EN + ZH edited (7 pairs, both sides):
  solo-founders-winning        → solo-founder-vs-cofounder, solo-saas-pricing
  how-to-get-first-users-solo  → seo-for-indie-hackers, rented-land-ai-search,
                                 when-to-hire-your-first-contractor
  what-agents-cant-do          → mcp-security-solo-builder, agents-fail-boringly,
                                 memory-is-the-product
  ship-one-tiny-thing          → ship-the-half-done-thing, first-charge-solo-builder,
                                 evenings-weekends-shipping
  done-is-a-claim              → mcp-security-solo-builder, agents-fail-boringly,
                                 agent-patterns-worth-knowing
  agent-patterns-worth-knowing → done-is-a-claim, memory-is-the-product,
                                 mcp-security-solo-builder
  reuse-vs-rebuild             → vibe-coding-hidden-cost, open-source-dependency
```

ZH links use **plain markdown paths** (`/zh/posts/<slug>/`), never `{{< ref >}}` — per the convention
correction logged 2026-09-20 (`ref` double-prefixes in ZH and hard-fails `REF_NOT_FOUND`).

```
AFTER                               EN    ZH
posts with 0 inbound internal links  10    10     (was 23 / 23)
```

**The 10 remaining orphans are deliberately left**: all are deep-tail posts with **no natural hub**
and — verified — **none hold GSC impressions**. Cross-checked against the v3 Candidate Queue; only
`execution-collapse` and `leaving-corporate-job-build-solo` are even mentioned there, neither as a
foothold. Another pass would mean manufacturing relevance, which is the failure mode this avoids.

## Finding 2 — 🔴 two ZH posts were written in SIMPLIFIED Chinese (unrelated, found while editing)

While adding the ZH links I noticed `how-to-get-first-users-solo`'s Sources block was Simplified.
Scoped it properly across all 38 ZH posts:

```
reuse-vs-rebuild.md            344 divergent chars of 2009 CJK   ← SIMPLIFIED
how-to-get-first-users-solo.md 326 divergent chars of 1988 CJK   ← SIMPLIFIED
(next worst: 2 chars — false positives)
```

**This breaks a stated site guardrail**: `i18n/zh.yaml` is Traditional and 35 of the 37 other ZH
posts are Traditional. It matters more than usual here because **ZH is this site's only converting
lane** (`ship-one-tiny-thing` at pos 1.0 is the site's sole converting page).

**Root cause — a test gap, not a one-off:** `tests/test_language_isolation.sh` guards EN-vs-ZH bleed
but has **no Traditional-vs-Simplified check at all**. A Simplified body passes every existing test.

**Fixed:**
1. Both files rewritten in **natural Traditional Chinese** (Taiwan/HK usage), not a glyph map —
   `使用者` not `用户`, `程式碼`, `函式庫`, `依賴`, `終止`, `走鐘`, `回饋`, `專案`, `建置`.
   Frontmatter values (title/tags/alt) converted; link paths, structure, bold/italic markers and
   `## Sources` headings preserved byte-identically.
2. **New guard added: `tests/check_zh_traditional.py`** — flags any ZH post with ≥10 characters from
   a curated Simplified-divergence set. Catches this class permanently.

⚠️ **The guard's first draft was itself buggy** and is worth recording: the divergence set included
`向`, `量`, `台`, `准`, `制`, which are **identical or legitimately interchangeable** in Traditional
(`內向`/`流量`/`控制` unchanged; `准許` is valid Traditional; `台`/`臺` are both correct). It therefore
flagged 4 *correct* Traditional posts as failures — a false-positive test that would have blocked all
future ZH work. Verified against OpenCC `s2t` and the set was narrowed. **A guard that cries wolf is
worse than no guard** — same lesson as the broken-probe pitfalls in `decade-content-pipeline`.

## Non-issues (verified, then corrected rather than acted on)

- ⚠️ **`{{< ref >}}` in `mcp-security-solo-builder` (EN) — NOT a bug.** I initially flagged this as a
  build risk given the 09-20 ZH correction. Verified: it is the only file using `ref`, **EN only**,
  `hugo --gc` reports no `REF_NOT_FOUND`, and it renders to exactly the same URLs as plain paths
  (`https://builderdecade.com/posts/api-vs-mcp/`). It is a **convention inconsistency, not a defect**.
  Left alone; worth normalising opportunistically, not worth a commit of its own.
- My first link-graph run reported 3 "broken links" from that same file — a **regex false positive**
  (my pattern required a trailing slash; `ref` shortcodes have none). Corrected before acting.

## Verification

```
hugo --gc                          clean, 0 errors (EN 273 / ZH 299 pages)
tests/check_zh_traditional.py      ✅ PASS — all 38 ZH posts Traditional (NEW guard)
tests/test_language_isolation.sh   ✅ PASS — RESULT: PASS
tests/check_no_stray_emphasis.py   ✅ PASS — 76 rendered posts
orphan recount                     EN 23 → 10 · ZH 23 → 10
built-HTML spot checks             EN+ZH new links render with correct hrefs ✓
```

## Not done (deliberately)

- **No new post.** The v3 gate found no candidate survives foothold + dedupe.
- **No external backlinks.** Per the v3 note, EN backlinking stays a proven-unknown on both sites.
- **No title/description/tags changes** — the meta-description sweep completed 2026-09-17.
- B (`what-your-agent-can-reach`) is untouched by this commit.
